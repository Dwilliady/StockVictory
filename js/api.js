const API = {

    baseUrl: "",


    async get(action, params = {}) {

        console.log(
            "API GET",
            action,
            params
        );

        // Nanti:
        // const response = await fetch(...)

        return null;
    },


    async post(action, data = {}) {

        console.log(
            "API POST",
            action,
            data
        );

        // Nanti:
        // const response = await fetch(...)

        return null;
    }

};